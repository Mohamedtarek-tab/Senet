server:
  port: 8082

spring:
  application:
    name: car-service
  datasource:
    url: jdbc:sqlite:${DB_PATH:./cars.db}
    driver-class-name: org.sqlite.JDBC
  jpa:
    database-platform: org.hibernate.community.dialect.SQLiteDialect
    hibernate:
      ddl-auto: update
    show-sql: true